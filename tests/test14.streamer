funk num sum(list<num> nums)
{
  num result = 0;
  for (num n : nums)
  {
    result = result + n;
  }
  return result;
}

funk void printResult(num result, text start)
{
  print start + "\n" + result;
}

print sum({5,6,7});
printResult(sum({86, 7, 5, 309}), "Result");
